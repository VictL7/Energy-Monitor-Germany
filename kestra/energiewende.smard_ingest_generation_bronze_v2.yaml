id: smard_ingest_generation_bronze_v2
namespace: energiewende

description: |
  Optimized version: Async concurrent HTTP + EachParallel + Batch upload
  v3: Fix bare string SyntaxError

inputs:
  - id: start_date
    type: STRING
    defaults: "2019-01-01"
  - id: end_date
    type: STRING
    defaults: "2026-01-01"

tasks:
  - id: iterate_energy_types
    type: io.kestra.plugin.core.flow.EachParallel
    value:
      - "4066" # biomass
      - "1226" # hydropower
      - "1225" # wind_offshore
      - "4067" # wind_onshore
      - "4068" # photovoltaics
      - "1228" # other_renewable
      - "1224" # nuclear
      - "1223" # lignite
      - "4069" # hard_coal
      - "4071" # fossil_gas
      - "4070" # hydro_pumped_storage
      - "1227" # other_conventional
    tasks:
      - id: download_and_upload
        type: io.kestra.plugin.scripts.python.Script
        taskRunner:
          type: io.kestra.plugin.scripts.runner.docker.Docker
        containerImage: python:3.11-slim
        pythonVersion: "3.11"
        beforeCommands:
          - pip install requests aiohttp pandas pyarrow google-cloud-storage --quiet
        env:
          FILTER_ID: "{{ taskrun.value }}"
          START_DATE: "{{ inputs.start_date }}"
          END_DATE: "{{ inputs.end_date }}"
          GCP_CREDS: "{{ kv('GCP_CREDS') }}"
          BUCKET_NAME: "{{ kv('GCP_BUCKET_NAME') }}"
        script: |
          import os, json, asyncio, sys
          import aiohttp
          import requests
          import pandas as pd
          from google.cloud import storage
          from google.oauth2 import service_account
          from google.api_core.retry import Retry

          # ── 1. 配置 ──────────────────────────────────
          ENERGY_MAP = {
              '4066': 'biomass',           '1226': 'hydropower',
              '1225': 'wind_offshore',     '4067': 'wind_onshore',
              '4068': 'photovoltaics',     '1228': 'other_renewable',
              '1224': 'nuclear',           '1223': 'lignite',
              '4069': 'hard_coal',         '4071': 'fossil_gas',
              '4070': 'hydro_pumped_storage', '1227': 'other_conventional'
          }
          CONCURRENCY_LIMIT = 20

          fid         = os.environ['FILTER_ID']
          fname       = ENERGY_MAP.get(fid, 'unknown')
          start_date  = os.environ['START_DATE']
          end_date    = os.environ['END_DATE']
          bucket_name = os.environ.get('BUCKET_NAME', '').strip()
          base_url    = "https://www.smard.de/app/chart_data"

          print(f"🚀 开始采集 [{fname}] (ID: {fid}): {start_date} → {end_date}")

          # ── 2. 获取时间戳索引 ─────────────────────────
          index_url = f"{base_url}/{fid}/DE/index_quarterhour.json"
          try:
              resp = requests.get(index_url, timeout=30)
              resp.raise_for_status()
              all_timestamps = resp.json().get('timestamps', [])
          except Exception as e:
              print(f"❌ 索引获取失败 [{fname}]: {e}")
              sys.exit(1)

          # ── 3. 筛选时间戳 ─────────────────────────────
          start_ts = int(pd.to_datetime(start_date).timestamp() * 1000)
          end_ts   = int(pd.to_datetime(end_date).timestamp() * 1000)

          relevant_ts = [ts for ts in all_timestamps if ts <= end_ts]
          start_idx = 0
          for i, ts in enumerate(relevant_ts):
              if ts >= start_ts:
                  start_idx = max(0, i - 1)
                  break
          relevant_ts = relevant_ts[start_idx:]
          print(f"📦 需要下载的时间戳块数: {len(relevant_ts)}")

          # ── 4. 异步并发下载 ───────────────────────────
          async def fetch_one(session, semaphore, url):
              async with semaphore:
                  try:
                      async with session.get(
                          url, timeout=aiohttp.ClientTimeout(total=30)
                      ) as r:
                          if r.status == 200:
                              return await r.json()
                  except Exception as e:
                      print(f"⚠️ 请求失败 {url}: {e}")
              return None

          async def fetch_all(ts_list):
              urls = [
                  f"{base_url}/{fid}/DE/{fid}_DE_quarterhour_{t}.json"
                  for t in ts_list
              ]
              semaphore = asyncio.Semaphore(CONCURRENCY_LIMIT)
              connector = aiohttp.TCPConnector(limit=CONCURRENCY_LIMIT)
              async with aiohttp.ClientSession(connector=connector) as session:
                  tasks = [fetch_one(session, semaphore, url) for url in urls]
                  return await asyncio.gather(*tasks)

          results = asyncio.run(fetch_all(relevant_ts))

          # ── 5. 解析合并 ───────────────────────────────
          rows = []
          for payload in results:
              if payload is None:
                  continue
              for item in payload.get('series', []):
                  if item[1] is not None:
                      rows.append({'timestamp': item[0], 'value': item[1]})

          if not rows:
              print(f"⚠️ [{fname}] 无数据，跳过")
              sys.exit(0)
          print(f"✅ 共收到 {len(rows)} 条原始记录")

          # ── 6. 清洗过滤 ───────────────────────────────
          df = pd.DataFrame(rows)
          df['datetime'] = pd.to_datetime(df['timestamp'], unit='ms', utc=True)
          mask = (
              (df['datetime'] >= pd.to_datetime(start_date).tz_localize('UTC')) &
              (df['datetime'] <= pd.to_datetime(end_date).tz_localize('UTC'))
          )
          df_final = (
              df.loc[mask]
                .sort_values('datetime')
                .rename(columns={'value': fname})
                [['datetime', fname]]
                .copy()
          )
          print(f"📊 过滤后: {len(df_final)} 条  |  {df_final['datetime'].min()} → {df_final['datetime'].max()}")

          # ──────────────────────────────────────
          # 7. 按年份分割并上传到 GCS
          # ──────────────────────────────────────
          for yr in range(pd.to_datetime(start_date).year, pd.to_datetime(end_date).year + 1):
              df_year = df_final[df_final['datetime'].dt.year == yr]
              if df_year.empty:
                  print(f"⚠️ [{fname}] {yr} 无数据，跳过")
                  continue

              local_file = f"generation_{fname}_{yr}.parquet"
              df_year.to_parquet(local_file, engine='pyarrow', index=False, compression='snappy')

              blob_path = (
                  f"raw/smard/generation/"
                  f"type={fname}/"
                  f"year={yr}/"
                  f"{yr}-01-01_to_{yr}-12-31.parquet"
              )

              # ──────────────────────────────────────
              # 8. 上传 GCS
              # ──────────────────────────────────────
              try:
                  creds = service_account.Credentials.from_service_account_info(
                      json.loads(os.environ['GCP_CREDS'])
                  )
                  client  = storage.Client(credentials=creds)
                  blob    = client.bucket(bucket_name).blob(blob_path)
                  blob.upload_from_filename(local_file, timeout=300, retry=Retry(deadline=300))
                  print(f"✅ 已上传: gs://{bucket_name}/{blob_path}")
              except Exception as e:
                  print(f"❌ 上传失败 [{fname}] {yr}: {e}")
                  sys.exit(1)
